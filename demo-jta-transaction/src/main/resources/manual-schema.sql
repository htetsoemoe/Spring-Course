

create table employee (
    id int,
    firstname varchar (32),
    lastname varchar (32),
    email varchar (32),
    phonenumber varchar (32),
    hiredate date,
    salary int
);

create table product (
    id int,
    name varchar (32),
    quantity int,
    price float,
    available boolean
);